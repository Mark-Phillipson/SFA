Can we make a plan to make the current application available as a Razor class library so it could be used to publish a phone application on android using MAUI?

I have created an empty razor class library project. The current application is a Blazor WebAssembly app with several components, services, and dependencies.

