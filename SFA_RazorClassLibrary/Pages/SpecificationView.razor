@page "/specification"
@inject HttpClient Http

<h3>Specification Document</h3>

@if (specText == null)
{
    <p>Loading...</p>
}
else
{
    <label class="visually-hidden" for="specTextArea">Specification document</label>
    <textarea id="specTextArea" readonly style="width:100%;height:600px;border:1px solid #ccc;padding:1rem;background:#fafafa;white-space:pre;">
@specText
    </textarea>
}

@code {
    private string specText = string.Empty;

    protected override async Task OnInitializedAsync()
    {
        try
        {
            specText = await Http.GetStringAsync("Specification.md");
        }
        catch
        {
            specText = "Specification markdown file not found.";
        }
    }
}
